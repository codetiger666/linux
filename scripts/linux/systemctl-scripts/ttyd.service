[Unit]
Description=ttyd
After=syslog.target
After=network.target

[Service]
Type=forking
PIDFile=/tmp/ttyd.pid
ExecStart=/usr/bin/ttyd -6 -c codetiger:密码 bash
ExecStop=/usr/bin/pkill ttyd
RestartSec=10s
TimeoutSec=0

[Install]
WantedBy=multi-user.target

